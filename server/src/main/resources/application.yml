spring:
  application:
    name: Smart Logistics Management System

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://binarybrains2025_db_user:uPiSoJk3Cdz3uqCM@cluster0.1heupdn.mongodb.net/slms_db?retryWrites=true&w=majority&appName=Cluster0}
      database: ${MONGODB_DATABASE:slms_db}

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

server:
  port: ${SERVER_PORT:8080}

# OpenAPI / Swagger
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: method
